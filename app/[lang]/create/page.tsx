import { NameWizard } from "@/components/name-form/name-wizard";

/**
 * Create page — 4-step name form wizard.
 * Renders the multi-step form for collecting user details
 * and generating personalized Chinese names.
 */
export default function CreatePage() {
  return <NameWizard />;
}
